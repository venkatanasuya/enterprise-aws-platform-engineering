# Load Balancing
Inventory contains 2 NLBs and 2 ALBs (the ALBs are Elastic Beanstalk-managed). Review whether EB-managed ALBs should remain owned by Elastic Beanstalk instead of standalone Terraform.
